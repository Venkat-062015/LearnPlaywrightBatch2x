let attempt = 0; // Initialization
while (attempt < 3) { // Condoition
    console.log(attempt);
    attempt++; // Updation
}

let modi = 1;
while (modi <= 15) { //1 to 15 times
    console.log("Modi will do 15+ years")
    modi++;

}